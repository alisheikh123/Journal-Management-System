@model IEnumerable<WebApplication9.Models.Class.AdmissionContext.tblActivitySheet>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.activityDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activityHead)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activitySubHead)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activityDetail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.activityDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activityHead)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activitySubHead)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activityDetail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
